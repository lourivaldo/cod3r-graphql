type Mutation {
    createUser(
        name: String
        email: String
        age: Int
    ): User!
    removeUser(id: Int!): User
    updateUser(
        id: Int!
        name: String
        email: String
        age: Int
    ): User
}
