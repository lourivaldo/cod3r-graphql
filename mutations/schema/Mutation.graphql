type Mutation {
    createUser(
        data: UserInput!
    ): User!
    removeUser(filter: UserFilter!): User
    updateUser(
        id: Int!
        name: String
        email: String
        age: Int
    ): User
}
