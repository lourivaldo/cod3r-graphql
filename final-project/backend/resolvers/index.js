const Query = require('./Query')
const Mutation = require('./Mutation')
const User = require('./Type/User')
const Role = require('./Type/Role')

module.exports = {
    Query,
    Mutation,
    User,
    Role
}
